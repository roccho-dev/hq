# CMP matcher CI

Refs #9.

This PR should keep the local completion UI evidence reproducible in the repository.

The CI job should build the small demo, produce text frames, render PNG frames, and upload a contact sheet artifact.

The interactive REPL remains future work.
