# Review

Use workflow status when reviewing terminal behavior.

Official product-runtime review must start from `cmd/hq` and `hq-*` binaries. Any `hq-reflective` name is historical proof or compatibility surface, not product authority.
