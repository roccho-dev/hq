# Review

Use workflow status when reviewing terminal behavior.

Official product-runtime review must start from `cmd/hq`, `hq-*` binaries, and the Official hq proof workflow.

There is no second official `hq-reflective` command in this branch. Historical reflective workflow files were removed after the official Linux/Windows proof workflow replaced them.
