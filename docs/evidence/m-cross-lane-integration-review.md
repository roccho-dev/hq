# Reviewer readback

> Actual `hq` accepted output enters one canonical worker path. Validation and exact approval precede adapter resolution; durable policy/result rows are redacted; one local workspace has one worker-process owner; result rows remain the only lifecycle authority and rebuild observation after restart.
